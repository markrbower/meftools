#include <stdio.h>
#include <stdlib.h>
#include <string.h>

using namespace std;

int main (int argc, const char * argv[]) {
    string filename = "";
    string password = "erlichda";
    int bufferSize = 1024;
    string subject = argv[1];
    string session = argv[2];

    processMEFexample( filename, password, suject, session, bufferSize, type, threshold, duration );


}
